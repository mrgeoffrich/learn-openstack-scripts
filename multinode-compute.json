{
  "provisioners": [
    {
      "type": "shell",
      "execute_command": "echo 'vagrant' | {{.Vars}} sudo -S -E bash '{{.Path}}'",
      "script": "scripts/mutlinode-compute.sh"
    }
  ],
  "builders": [
    {
      "type": "virtualbox-ovf",
      "source_path": "./virtualbox/base-ubuntu/base-ubuntu.ovf",
      "headless": true,
      "ssh_username": "{{ user `ssh_username` }}",
      "ssh_password": "{{ user `ssh_password` }}",
      "shutdown_command": "echo 'vagrant' | sudo -S shutdown -P now",
      "vm_name":"os-compute-server",
      "output_directory": "virtualbox/os-compute-server"
    },
    {
      "type": "vmware-vmx",
      "source_path": "./vmware/base-ubuntu/base-ubuntu.vmx",
      "headless": true,
      "ssh_username": "{{ user `ssh_username` }}",
      "ssh_password": "{{ user `ssh_password` }}",
      "shutdown_command": "echo 'vagrant' | sudo -S shutdown -P now",
      "vm_name":"os-compute-server",
      "output_directory": "vmware/os-compute-server",
      "vmx_data": {
        "displayname":"OpenStack Compute Server"
      }
    }
  ],
  "variables": {
    "ssh_password": "vagrant",
    "ssh_username": "vagrant"
  }
}